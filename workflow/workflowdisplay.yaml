name: massql_workflow
displayname: STUFF
Views:
-   name: Pairs List
    displayname: Pairs List
    viewname: pairslist
    displaytype: datatable_serverbig
    parameters:
        filename: nf_output/merged_pairs.tsv
        columns:
            -   title: "View"
                data: 
            -   title: "CLUSTERID1"
                data: CLUSTERID1
            -   title: "CLUSTERID2"
                data: CLUSTERID2
            -   title: "DeltaMZ"
                data: DeltaMZ
            -   title: "DeltaMZ"
                data: DeltaMZ
            -   title: "Cosine"
                data: Cosine
            -   title: "MatchedPeaks"
                data: MatchedPeaks
        columnDefs: '[ {"targets": 0,"data": null,"render": function ( data, type, row, meta ) {
            return `
            <a target="_blank" href="/dashboards/lcms?usi=mzspec:GNPS2-${task}-${encodeURIComponent(row["filename"])}">None</a>
            `;}}]'
            
